# presswire Python 依赖
#
# 注: 任务清单中的 "typst-py" 是社区俗称，PyPI 上的实际包名是 `typst`
# （提供 `import typst` 的 Python 绑定）。typst CLI 已全局安装于
# /usr/local/bin/typst (v0.15.1)，无需在此重复安装命令行工具。
typst>=0.15.0
pypdf>=6.14.2
pillow>=12.3.0
fonttools>=4.63.0
